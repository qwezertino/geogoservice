# Compiled binary (local dev builds inside app/)
app/geogoservice
app/cmd/server/server

# Go module cache
vendor/

# Local persistent data (postgres, minio) - ignore contents but keep directory structure
data/**
!data/**/.gitkeep

# Environment secrets (never commit real credentials)
.env

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker build cache
*.tar

# Test coverage
*.out
coverage.html
